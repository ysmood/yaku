module.exports = class Promise
	constructor: (executor) ->

	then: (onFulfilled, onRejected) ->

	catch: (onRejected) ->

	@all: (iterable) ->

	@race: (iterable) ->

	@reject: (reason) ->

	@resolve: (value) ->
